import { BagIcon, LeafMark, MenuIcon } from './icons'

const navigation = ['Shop', 'Our Story', 'Ingredients', 'Journal']
const Header = ({ cartCount, menuOpen, setMenuOpen }) => <header className="relative z-20 border-b border-[#d8d1bc] bg-[#fbf7ef]/95 backdrop-blur"><div className="announcement">Complimentary shipping on orders above ₹999 <span>✦</span> Handmade with intention</div><div className="site-shell flex h-[76px] items-center justify-between"><button className="menu-button" type="button" onClick={() => setMenuOpen(!menuOpen)} aria-label="Open navigation"><MenuIcon className="h-6 w-6" /></button><a className="brand-logo" href="#home" aria-label="Twach Veda home"><LeafMark className="h-9 w-9" /><span>Twach<span>Veda</span></span></a><nav className="desktop-nav" aria-label="Main navigation">{navigation.map((item) => <a key={item} href={`#${item.toLowerCase().replace(' ', '-')}`}>{item}</a>)}</nav><a className="cart-button" href="#shop" aria-label={`${cartCount} products in shopping bag`}><BagIcon className="h-6 w-6" /><span className="cart-label">Bag</span>{cartCount > 0 && <span className="cart-badge">{cartCount}</span>}</a></div>{menuOpen && <nav className="mobile-nav">{navigation.map((item) => <a onClick={() => setMenuOpen(false)} key={item} href={`#${item.toLowerCase().replace(' ', '-')}`}>{item}</a>)}</nav>}</header>
export default Header
